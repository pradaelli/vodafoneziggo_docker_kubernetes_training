# Default values for config-api.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: sunnybikes
  pullPolicy: IfNotPresent
  tag: latest
